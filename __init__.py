"""Motuva unified ML pipeline (Modules 1 + 2 + 3 fused into one package).

Run with this package's directory on ``sys.path`` (top-level subpackages so the
reused Module-3 absolute imports — ``from config...`` / ``from utils...`` —
keep resolving). See README.md and SETUP.md.
"""

__version__ = "0.1.0"
